<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="onNavigatedTo">
<<<<<<< HEAD
  <StackLayout>
    <Label text="{{selectedMovie.title}}" class="title"/>
  </StackLayout>
=======
	<Page.actionBar>
		<ActionBar title="Experience" class="actionBar"></ActionBar>
	</Page.actionBar>
	<TabView class="cell">
		<TabView.items>			
			<TabViewItem title="Notes">
				<TabViewItem.view>
					<StackLayout>
						<TextView id="noteWriteField" hint="Type your note here." />
						<Button text="Add note" class="expButtons" id="noteButton" onTap="saveNote" />
						<Label id="title" class="textLabel" text="Notes" />
						<StackLayout class="cell">
							<ListView items="{{ notes }}" onLongPress = "deleteNote">
								<ListView.itemTemplate>
									<Label text="{{ string }}" class="textLabel" textWrap="true"/>
								</ListView.itemTemplate>
							</ListView>
						</StackLayout>	
					</StackLayout>
				</TabViewItem.view>
			</TabViewItem>
			<TabViewItem title="Pictures">
				<TabViewItem.view>
					<StackLayout>	
						<Image id="image" />					
						<Button text="Take Picture" class="expButtons" id="pictureButton" onTap="onTakePictureTap" />
						<StackLayout class="cell">
							<ListView items="{{ pictureList }}">
								<ListView.itemTemplate>
									<Label text="{{ img }}" class="textLabel"/>
								</ListView.itemTemplate>
							</ListView>
						</StackLayout>		
					</StackLayout>
				</TabViewItem.view>
			</TabViewItem>
			<TabViewItem title="Rate">
				<TabViewItem.view>
					<StackLayout>
						<Label text="Rate this movie:" class="sliderLabel" textWrap="true" />
						<Slider minValue="{{ minValue }}" maxValue="{{ maxValue }}" value=" {{ sliderValue }}" />
						<Label text="My rate is:" class="sliderLabel" />
                        <Label text="{{ sliderValue }}" class="sliderLabel" />	
					</StackLayout>       
				</TabViewItem.view>
			</TabViewItem>
		</TabView.items>
	</TabView>
>>>>>>> 1a5d771ca41386506addc82500c26a717eecb761
</Page>
